import React from "react";
import { Link } from "react-router-dom";
import SectionTitle from "../../components/layout/section/SectionTitle";
import Container from "../../components/layout/Container";

const ReactHookForm = () => {
  return (
    <div className="pt-[10%] relatve">
      <SectionTitle title="react-hook-form"></SectionTitle>
      <Container className="flex flex-wrap mb-20 lg:gap-10 lg:flex-nowrap"></Container>
    </div>
  );
};

export default ReactHookForm;
